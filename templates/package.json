{
	"name": "<%= appName %>",
	"version": "<%= appVersion %>",
	"description": "<%= appDesc %>",
	"main": "dist/server.js",
	"scripts": {
		"start": "node dist/server.js",
		"test": "echo \"Error: no test specified\" && exit 1",
		"dev": "gulp dev"
	},
	"devDependencies": {
		"babel-preset-es2015": "^6.18.0",
    "gulp": "^3.9.1",
    "gulp-babel": "^6.1.2",
    "gulp-eslint": "^3.0.1",
    "gulp-nodemon": "^2.2.1"
	},
	"dependencies": {
		"debug": "^2.3.3",
		"express": "^4.14.0"
	}
}
