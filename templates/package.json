{
	"name": "<%= appName %>",
	"version": "<%= appVersion %>",
	"description": "<%= appDesc %>",
	"main": "dist/server.js",
	"scripts": {
		"start": "node dist/server.js",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"devDependencies": {
		"babel-preset-es2015": "^6.18.0",
		"gulp": "^3.9.1",
		"gulp-babel": "^6.1.2",
		"gulp-nodemon": "^2.2.1"
	},
	"dependencies": {
		"express": "^4.14.0"
	}
}
